import React from 'react';
import { View, StyleSheet } from 'react-native';

function Test(props) {
    return (
        <View>

        </View>
    );
}

const styles = StyleSheet.create({
    container: {},
})

export default Test;